{
  "name": "workers-sample",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "$npm_execpath run build && vite preview",
    "deploy": "$npm_execpath run build && wrangler deploy",
    "build:staging": "CLOUDFLARE_ENV=staging vite build",
    "deploy:staging": "$npm_execpath run build:staging && wrangler deploy --env staging",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.30"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.44.0",
    "vite": "^8.1.4",
    "vite-ssr-components": "^0.6.1",
    "wrangler": "^4.110.0"
  }
}